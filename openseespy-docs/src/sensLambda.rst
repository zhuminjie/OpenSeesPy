.. include:: sub.txt

======================
 sensLambda command
======================

.. function:: sensLambda(patternTag, paramTag)

   Returns the current load factor sensitivity to a parameter in a load pattern.

   ========================   ===========================================================================
   ``patternTag`` |int|       load pattern tag
   ``paramTag`` |int|         parameter tag
   ========================   ===========================================================================
