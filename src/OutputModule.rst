.. include:: sub.txt

===============
 Output Module
===============

- View Setting Module is for showing the output when :doc:`runAllModel` or :doc:`runOneModel` is called.

- View Setting Module itself is a :doc:`keyboard-shortcuts`.

- To clear the output, delete all contents in the module.
