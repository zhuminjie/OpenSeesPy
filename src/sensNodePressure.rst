.. include:: sub.txt

==========================
 sensNodePressure command
==========================

.. function:: sensNodePressure(nodeTag, paramTag)

   Returns the current pressure sensitivity to a parameter at a specified node.

   ========================   ===========================================================================
   ``nodeTag`` |int|          node tag
   ``paramTag`` |int|         parameter tag
   ========================   ===========================================================================
